"use strict";
jQuery.fn.flyButton = function (s) {
    var t = s.class, i = (s.style, s.text), e = s.iconClass, n = s.spin, a = s.events;
    if (this.addClass(t), null != n || "" != n) {
        var r = jQuery("<i>", {class: n, style: "display: none;", id: this[0].getAttribute("id") + "Spinner"});
        this.append(r)
    }
    var l = jQuery("<i>", {class: e}), u = jQuery("<span>", {text: i});
    this.append(l, u);
    for (var d in a) {
        var p = a[d];
        this.bind(d, p)
    }
};